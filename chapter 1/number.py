import this 
total=2+3*4,(2+3)*4, 0.1+0.1,3*0.1
print(total)
age=23
msg="happy "+ str(age) +" rd birthday"#variable inside the variable we can not print we can use str() that is only can be interpret identify
print(msg)
